# PLANTED FACTS:
# - Duplicate entries in __all__ should not cause errors
# - helper should get at least one reference (not two — dedup is optional)
from utils import helper

__all__ = ["helper", "helper"]
